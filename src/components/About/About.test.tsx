import { render } from '@testing-library/react';
import About from './About';

describe('<About/>', () => {
  beforeEach(() => {
    render(<About />);
  });

  it('Should display the block title', () => {});
});
